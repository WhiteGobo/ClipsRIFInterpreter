Document(
    Prefix(ex <http://example.com/>)
    Group (
      Forall ?frame (
        If And (
          ex:find-frame(?frame)
          Not (Exists ?x (
            ?frame[ex:b -> ?x]
          ))
        ) Then ex:ok()
      )
    )
)
