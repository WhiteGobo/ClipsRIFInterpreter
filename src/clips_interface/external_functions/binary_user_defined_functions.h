#pragma once

#include <clips.h>

//void rif_is_literal_hexBinary(Environment *env, UDFContext *udfc, UDFValue *out);
void rif_is_literal_base64Binary(Environment *env, UDFContext *udfc, UDFValue *out);
//void rif_cast_base64Binary(Environment *env, UDFContext *udfc, UDFValue *out);
